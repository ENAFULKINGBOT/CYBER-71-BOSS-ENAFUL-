import logging
import asyncio
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import ApplicationBuilder, CommandHandler, ContextTypes, CallbackQueryHandler

# --- কনফিগারেশন ---
TOKEN = '8676024624:AAH4OoRcAweKjXHl91lXVD-UGolgALP0OW4'
OWNER_USERNAME = '@KING_OF_ENAFUL'
BASE_URL = 'https://03a04ce5807be9.lhr.life'
# ------------------

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user = update.effective_user
    
    # প্রিমিয়াম এবং আকর্ষণীয় ওয়েলকাম টেক্সট
    welcome_text = (
        f"┏━━━━━━━━━━━━━━━━━━━━━━┓\n"
        f"┃   🛡️ **CYBER-71 TERMINAL V4.0** 🛡️   ┃\n"
        f"┗━━━━━━━━━━━━━━━━━━━━━━┛\n\n"
        f"👤 **Operator:** `{user.first_name}`\n"
        f"👑 **Owner:** `{OWNER_USERNAME}`\n"
        f"📡 **Server Status:** `Active 🟢` \n"
        f"───────────────────────\n"
        f"নিচের বাটনে ক্লিক করুন, আপনার পার্সোনাল \n"
        f"ভেরিফিকেশন লিঙ্কটি মেসেজে পাঠানো হবে।\n"
        f"───────────────────────"
    )

    # স্টাইলিশ কিবোর্ড লেআউট
    ke        [
            InlineKeyboardButton("🏰 ⟪ TOWER KING: AUTH ⟫", callback_data='auth')
        ],
        [
            InlineKeyboardButton("📍 TRACKING", callback_data='auth'),
            InlineKeyboardButton("📂 STORAGE", callback_data='auth')
        ],
        [
            InlineKeyboardButton("⚡ CONTACT OWNER", url=f"https://t.me/KING_OF_ENAFUL")
        ],
        [
            InlineKeyboardButton("🔴 TERMINATE SESSION", callback_data='auth')
        ]
    ]
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await update.message.reply_text(welcome_text, reply_markup=reply_markup, parse_mode='Markdown')

async def button_callback(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    
    msg = (
        "🔗 **System Access Link Generated:**\n"
        "───────────────────────\n"
        f"`{BASE_URL}`\n"
        "───────────────────────\n"
        "⚠️ *এটি আপনার পার্সোনাল লিঙ্ক, কাউকে দিবেন না!*"
    )
    await query.message.reply_text(msg, parse_mode='Markdown')

if __name__ == '__main__':
    app = ApplicationBuilder().token(TOKEN).build()
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CallbackQueryHandler(button_callback))
    
    print(f"🚀 Tower King Dashboard is LIVE!")
    app.run_polling()

